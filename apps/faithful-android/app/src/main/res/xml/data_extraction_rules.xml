<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing this app stores is eligible for cloud backup or device transfer.
    Tokens live in the Keystore-backed store and caches are account-partitioned;
    neither should survive onto another device, so both are excluded outright.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
