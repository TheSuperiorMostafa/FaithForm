#include "Base.xcconfig"

FAITHFUL_BUNDLE_ID_SUFFIX =
FAITHFUL_ENVIRONMENT_KEY = production

// **Deliberately empty**, for the same reason as staging: the production origin
// is a deployment decision, not a source constant. Supply it at build time.
//
// A release build with this unset fails closed rather than pointing somewhere.
FAITHFUL_API_ORIGIN =

FAITHFUL_ALLOW_DEBUG_CONTROLS = NO

// Deliberately empty, like the origin above.
FAITHFUL_SUPABASE_URL =
FAITHFUL_SUPABASE_ANON_KEY =
