import { GroupPanelSkeleton } from "@/components/groups/skeletons";

/** Only the tab content: the group header and tabs stay on screen above it. */
export default function Loading() {
  return <GroupPanelSkeleton />;
}
