import { GroupsSectionBodySkeleton } from "@/components/groups/skeletons";

/** Only the content: the header and section links stay on screen above it. */
export default function Loading() {
  return <GroupsSectionBodySkeleton />;
}
